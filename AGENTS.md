# Chaos 项目协作规则

本文件只保留 Chaos 项目长期有效的协作原则。产品现状、版本进度和跨设备状态分别由对应文档记录，不在这里重复。

## 用户决策与冲突处理

- 用户是 Chaos 项目的最高决策者。
- Codex 可以并且应当提出产品、交互、架构、数据和风险建议。
- 用户明确确认的项目决定优先于仓库中的旧文档。
- 如果用户要求与现有文档、实现或历史决策冲突，先说明冲突和影响，向用户确认；确认后按用户决定执行，并同步所有受影响文档。
- 不自行用过期规则否定用户已经确认的需求，也不在存在实质歧义时擅自猜测。

## 开始工作

- 每次先阅读本文件，再按任务内容阅读相关文档和源码，不要求无差别读取全部文档。
- `docs/ROADMAP.md` 只提供版本进度参考，不是开发准入条件。
- 当需求超出当前版本参考范围时，向用户提示影响和建议，由用户决定是否继续。
- 当前任务以用户在当前窗口明确确认的要求为准。
- 编辑前检查当前分支和工作区状态，发现已有修改时保留并与其协作，不覆盖或回滚未确认的改动。

## 产品原则

Chaos 是本地优先的长篇小说桌面编辑器，不是营销页、通用后台或 AI 聊天壳。

长期优先级：

1. 稳定写作。
2. 本地保存。
3. 结构清晰。
4. AI 辅助。
5. 自动化。

- UI 应接近成熟写作工具，保持安静、低噪音和正文优先。
- 书架与书内工作区保持清晰分层；大纲和故事线可以独占主界面。
- AI 默认辅助作者，不擅自改写人物、伏笔、事件或世界观。
- 页面视觉效果默认由用户验证；除非用户明确要求，不主动启动浏览器、截图或进行视觉验收。

## 技术与数据边界

- 桌面壳使用 Electron，前端使用 Vue 3、TypeScript 和 Vite，编辑器使用 Tiptap / ProseMirror，状态管理使用 Pinia。
- 当前 SQLite 驱动为 `better-sqlite3`，当前迁移和查询使用显式 SQL。未来是否增加类型化查询层，应根据真实数据复杂度单独决策，不作为全局规则。
- SQLite 是结构化数据的事实来源；章节正文以 ProseMirror JSON 为权威内容；Markdown 是可读镜像。
- 文件系统、SQLite、系统密钥、导出和其它本地能力收敛在 Electron main process。
- Renderer 只能通过 preload 暴露的受控 API 使用本地能力，不直接访问 Node.js 文件系统或数据库。
- 项目路径、数据库和镜像文件必须保持在项目边界内，重要写入失败不能静默吞掉。

## 实现习惯

- 优先复用现有组件、类型、CSS 变量、服务边界和本地模式。
- 不夹带与用户需求无关的重构，不为了顺手大规模重排目录或更换技术栈。
- 结构化数据发生实际变化时，同步考虑类型、SQLite schema、迁移、读写映射和相关测试，但不把未来草案当成当前实现要求。
- 用户通常自行保持开发服务运行；不要默认启动或重启。确需运行态检查时，先复用现有服务和端口。

## 文档同步

项目文档必须与功能和代码实时同步。新增、修改或删除功能，以及改变交互流程、错误处理、数据语义、接口、架构或产品决定时，必须在同一次任务中检查并更新所有实际受影响的文档，不能把文档更新留到以后，也不能在文档仍描述旧行为时宣布任务完成。

- 开始实现前，先确认相关文档描述是否与当前代码一致；如果不一致，先向用户说明。
- 实现过程中一旦行为或方案发生变化，同步修改对应文档，不等到跨设备交接时补写。
- 功能完成后再次检查产品行为、用户流程、当前进度、架构、数据契约和重要决策是否一致。
- 只更新真正受影响的文档，不复制同一份事实到所有文件。

- `docs/PRODUCT.md`：产品愿景和长期体验方向。
- `docs/ROADMAP.md`：长期版本路线、各版本范围和进度快照。
- `docs/PROGRESS.md`：当前真实实现、未完成能力和下一步状态。
- `docs/UX_FLOWS.md`：当前用户流程、系统反馈和失败路径。
- `docs/HANDOFF.md`：只记录跨设备交接所需信息。
- `docs/DECISIONS.md`：只记录重要且需要长期追溯的产品或技术选择。
- `docs/ARCHITECTURE.md`、`docs/DATA_MODEL.md`、`docs/EDITOR_MODEL.md`：分别记录当前实现与目标架构、已落库模型与未来草案、正文模型。

## Git 与跨设备

- `main` 保持稳定，`develop` 作为当前持续开发和跨设备同步分支；不要求固定通过 PR 合并，具体同步方式由用户决定。
- GitHub 远端和仓库文档是跨设备开发事实来源，聊天记录不能作为唯一上下文。
- 不提交 `node_modules/`、`out/`、`dist/`、`.pnpm-store/`、Electron 打包产物、真实小说项目、数据库文件或密钥。
- 不自动执行 `stash`、强制合并、`reset` 或覆盖本地修改；出现分叉或脏工作区时先向用户说明。
- `handoff-in`、`handoff-out` 脚本和 CI 是否进一步简化，等待用户单独决定。
